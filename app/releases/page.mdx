---
title: Relizlar
description: Xinux operatsion tizimining barcha relizlari va o'zgarishlar jurnali
---

import { ReleaseCard, ReleaseGrid } from '../../components/ReleaseCard'
import { releases } from '../../data/releases'

# Reliz eslatmalari

<ReleaseGrid>
  {releases.map((r) => (
    <ReleaseCard key={r.version} {...r} />
  ))}
</ReleaseGrid>
