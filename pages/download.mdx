import Image from "next/image";
import { Card, Cards } from "@components/card";

import cinnamon from "public/showcase/cinnamon.jpg";
import i3wm from "public/showcase/i3wm.jpg";

{<h1 className="mt-10 mb-4 text-center text-[2.5rem] font-bold tracking-tight">Yuklash</h1>}

{<p className="mb-16 text-center text-lg text-gray-500 dark:text-gray-400">Xinux turlari va ularni yuklash sahifasi</p>}

<Cards num={3}>
  {[
    {
      name: "Cinnamon",
      url: "http://mirror.dc.uz/xinux/iso/alpha",
      image: cinnamon,
    },
    {
      name: "i3WM",
      url: "http://mirror.dc.uz/xinux/iso/beta",
      image: i3wm,
    },
  ].map(({ name, url, image }) => (
    <Card key={name} image arrow target="_blank" title={name} href={url}>
      <Image
        src={image}
        alt={`${name} preview`}
        className="object-cover [aspect-ratio:12/6.3]"
      />
    </Card>
  ))}
</Cards>
